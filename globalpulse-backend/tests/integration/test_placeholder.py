"""
Integration tests placeholder.
These tests will run against a live Finnhub API key and a running database.
They are skipped in CI unless GLOBALPULSE_INTEGRATION=1 is set.

Phase 1 integration tests are not included here — see unit tests for full coverage.
"""
